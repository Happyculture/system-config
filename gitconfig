[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto
  ui = true
[core]
  editor = vim
# For windows users
# fileMode = false # ignore file mode (chmod)
# autocrlf = true # handle CR and LF
[alias]
  up = pull --rebase
  st = status
  sl = log --graph --pretty=oneline --abbrev-commit --decorate
[branch]
# Some of us want to explicitly ask for rebase
#	autosetuprebase = always
[diff]
  algorithm = patience
